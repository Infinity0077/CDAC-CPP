# Day 05 Assignment (from class notes)

1. Create `Student` class and print marksheet for two students.
2. Create `Bank_fd` class for 3 account holders:
   - `si = p*r*n/100`
   - `amount = p + si`
3. Demonstrate constructor overloading:
   - default constructor
   - parameterized constructor
   - copy constructor
4. Employee attendance-salary update:
   - if present days >= 200, salary += 5000
   - else salary += 2500
   - do for two employee objects
